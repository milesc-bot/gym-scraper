# ─── Supabase ───────────────────────────────────────────────
# Project URL (e.g. https://xyzcompany.supabase.co)
SUPABASE_URL=

# Service-role key – has full DB access, never expose to the client.
# Use the anon key instead if your RLS policies allow the required writes.
SUPABASE_SERVICE_ROLE_KEY=

# ─── Bot identity & compliance ──────────────────────────────
# Custom User-Agent string.  Transparent identification lets site
# operators whitelist or contact you.
BOT_USER_AGENT=MilesC-GymBot/1.0 (+http://your-site.com/bot)

# Minimum delay (ms) between requests to the same domain.
RATE_LIMIT_MS=2000

# ─── LLM (Navigation Planner) ──────────────────────────────
# OpenAI API key for the self-healing navigation planner.
# The planner uses gpt-4o-mini (~$0.0003 per call).
OPENAI_API_KEY=

# Maximum cumulative LLM spend in cents before the planner stops.
LLM_BUDGET_CENTS=50

# ─── Authentication (Session Manager) ──────────────────────
# Credentials for gym sites that require login.
# Only needed if the target site gates its schedule behind auth.
GYM_USERNAME=
GYM_PASSWORD=

# TOTP secret (base32) for 2FA.  This is the shared secret from
# the QR code setup, NOT the 6-digit code itself.
# Example: JBSWY3DPEHPK3PXP
GYM_TOTP_SECRET=

# How long (hours) to keep saved cookies before re-authenticating.
COOKIE_TTL_HOURS=24

# ─── Trap detection ────────────────────────────────────────
# Maximum crawl depth before aborting a link chain.
MAX_CRAWL_DEPTH=5
